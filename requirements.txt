# ORAC STT - Production Dependencies
# These are the core dependencies needed to run the service in production.
# For development, use: pip install -r requirements-dev.txt
# For PyTorch backend (optional), use: pip install -r requirements-pytorch.txt

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Configuration
toml==0.10.2
tomli==2.0.1  # Fallback for Python < 3.11
PyYAML==6.0.1

# Metrics
prometheus-client==0.19.0

# Audio processing
numpy==1.24.3
scipy==1.11.4
librosa==0.10.1
soundfile==0.12.1

# HTTP client for ORAC Core integration
aiohttp==3.9.1
